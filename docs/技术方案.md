# 技术方案

> 本文档阐述 Mimir 的整体技术方案：要解决的问题、核心思路、各优化方向的设计与预期效果。
> 状态：**核心方向为三篇融合,三篇均已落地验证** = Continuum TTL(已 port) + LMCache offload(已集成) + CacheGen 压缩(已验证);
> 外部层工具外置/压缩 + 分层 + CoW + DeepSeek 评估已实现(支撑评测,非创新核心)。block-class 已弃用为核心(见 §3.7 注)。三篇机制跑通但并发多轮 agent 的端到端收益 benchmark 为下一步。

## 1. 问题分析：智能体推理的内存特征

智能体推理相较传统线性生成（如 vLLM 面向的批式解码），在内存使用上呈现以下显著特征，
每一项都对应潜在的优化空间：

| 特征 | 描述 | 带来的问题 | Mimir 应对模块 |
| --- | --- | --- | --- |
| KV Cache 持续累积 | 多轮交互不重置上下文 | 显存随轮次线性增长直至 OOM | `mimir.kv_cache` |
| 上下文高度冗余 | system prompt / 工具描述逐轮重复 | 重复计算与重复存储 | `mimir.context` |
| 推理路径分支 | 多候选决策树 / Tree-of-Thought | 分支导致 KV 指数膨胀 | `mimir.branch` |
| 工具中间数据 | 工具返回大 JSON / 长文本 | 全量进入 KV，挤占显存 | `mimir.tools` |
| 生命周期长 | 单任务可执行数十~数百步 | 资源长期占用、工具调用暂停时 KV 被挤掉致重 prefill | `mimir.kv_cache` / `mimir.tiered` |
| 异构硬件 | CUDA / DTK / CANN 多后端 | 抽象与适配成本高 | `mimir.hardware` |

## 2. 总体目标

在**保证任务成功率基本不下降**的前提下：

1. **降低显存峰值占用**（核心指标，对应评分 40 分）
2. **降低端到端推理延迟**（核心指标，对应评分 40 分）
3. **保持方法通用性**，可适配不同模型规模（Qwen 1.5B/7B、MiniCPM3-4B 等）与不同推理框架
4. **具备可复现性**，统一的 Benchmark 与相同硬件配置

## 3. 核心技术路线

### 3.1 KV Cache 生命周期管理（`mimir.kv_cache`）

本方向以三篇融合为主（详见 §3.7），核心是工具调用场景下的统一 KV 放置管线：

- **前缀复用（Prefix Caching）**：识别多轮对话 / Agent Loop 中稳定不变的上下文前缀（system
  prompt、few-shot、工具 schema），仅计算一次并在后续轮次复用其 KV。
- **工具调用边界 TTL 保留（Continuum，已 port）**：agent 暂停调工具时，给其 KV 挂 TTL 保留，
  同一 agent 程序下一步进来时 KV 还在、免重 prefill；TTL 到期或显存紧时释放。
- **分层 offload 搬运（LMCache，已集成）**：TTL 到期/显存紧时不直接丢，而是 offload 到 CPU/磁盘，
  下一步回来时 reload 而非重 prefill（layer-wise 与计算重叠）。经 `lmcache_compat.py` 接入。
- **KV 编解码压缩（CacheGen，已验证）**：offload 时把 KV 张量用 delta+分层量化+算术编码压成
  bitstream（合成 KV 实测 2.88×、论文真实 KV 3.5–4.3×），reload 时解码——使低带宽下 reload 仍快于重 prefill。
  编解码器已随 LMCache 0.4.7 一并发布（作为 storage serde 后端），`test_cachegen_serde.py` 验证 round-trip。

> 注：早期版本的「任务边界主动回收 / used_blocks→0」机制已删除——系任务结束后瞬时计数偷换
> （推理时该占仍占、回收重算拖慢服务），已彻底移除，由上述三篇融合的 sound 机制取代。
> 早期的 block-class「按语义角色淘汰」机制经重审不再作为核心创新（见 §3.7 注），代码暂留备查。

**预期效果**：长对话场景下重复前缀的 prefill 时间趋近于零；工具调用后下一步免重 prefill（TTL 命中）
或快速 reload（offload 命中，CacheGen 压缩使带宽不再是瓶颈）；显存压力下 KV 压缩外置而非崩溃。

### 3.2 分支推理内存共享（`mimir.branch`）

- **Tree-based KV with Copy-on-Write**：将分支推理组织为树结构，共享公共前缀的 KV 块。
- **CoW 语义**：仅当某分支真正修改（重新生成）对应 token 的 KV 时才物理拷贝，其余共享。
- **分支裁剪与回收**：被剪枝的分支 KV 立即回收。

**预期效果**：N 路分支的显存从 O(N·L) 降至接近 O(L + N·Δ)，Δ 为分支差异长度。

### 3.3 Prompt 与上下文压缩（`mimir.context`）

- **冗余检测与去重**：对 system prompt、工具描述等静态内容做哈希指纹，跨轮复用。
- **语义压缩**：对历史对话 / 工具返回进行摘要或结构化精简（可选，保真度可控）。
- **静态/动态分离**：静态内容复用前缀 KV，动态内容增量处理。

**预期效果**：减少 prefill 计算量与 KV 占用，同时不损失关键信息。

### 3.4 工具调用数据优化（`mimir.tools`）

- **结构化外置存储**：工具返回的大对象（长 JSON / 表格 / 图像）存入外部 store，仅在 KV 中
  保留引用与摘要。
- **按需加载（lazy materialization）**：仅在后续步骤真正引用时才展开。
- **截断与回填策略**：超长返回按策略截断，需要时回填。

**预期效果**：工具中间数据不再整体进入 KV Cache，显存占用大幅下降。

### 3.5 分层内存与异构存储（`mimir.tiered`）

- **三级分层**：GPU 显存（热）→ 主存（温）→ 外部存储（冷），类似 CPU L1/L2/L3。
- **冷热分离与动态迁移**：基于访问频率与生命周期预测，自动在层级间迁移 KV。
- **按需加载 / 重计算权衡**：冷数据可按需 reload 或按需重算，权衡访存与算力。

**预期效果**：突破单卡显存限制，支撑超长生命周期任务。

### 3.6 异构 AI 加轾件支持（`mimir.hardware`）

- **设备抽象层**：统一 `Device` 抽象，屏蔽 CUDA / DTK（昇腾） / CANN 差异。
- **后端探测与降级**：运行时探测可用后端，无 GPU 时降级到 CPU / offload。
- **国产化算力适配**：针对国产 NPU（昇腾 / 寒武纪等）的算子与内存模型适配。

**预期效果**：在 CUDA 与国产化算力环境下均可运行，提升兼容性（对应评分 20 分）。

### 3.7 创新核心：三篇融合 —— 工具调用场景下的统一 KV 放置管线

**定位**：Mimir 不自造机制，而是把三篇互补的 prior work 在 **agent 工具调用场景**下串成一条统一管线，并讲好这个故事。三篇各管 KV 生命周期的不同环节，融合后才是完整方案：

| 环节 | 论文 | 贡献 | 单独的缺口 |
| --- | --- | --- | --- |
| **何时**留/放 | Continuum（arXiv 2511.02230） | 工具调用边界 + TTL 保留 + cost-benefit | offload 整个未压缩张量，搬得慢 |
| **搬去哪/怎么搬** | LMCache（arXiv 2510.09665） | GPU↔CPU↔磁盘分层 offload、layer-wise 重叠、pin/lookup | 通用搬运工，搬的是原始张量 |
| **怎么压/怎么传得快** | CacheGen（arXiv 2310.07240, SIGCOMM'24） | delta+分层量化+算术编码把 KV 压成 bitstream（3.5–4.3× 压、3.2–3.7× 快） | 只解决"压/传"，不解决"何时留/放、搬去哪" |

**融合管线**：agent 某步发出工具调用 → Continuum 决定保留该步 KV（TTL pin GPU）→ 显存紧时 LMCache 把它 offload 到 CPU → **offload 的不是原始张量而是 CacheGen 编码的 bitstream**（传/存都小一个量级）→ 同一 agent 的下一步回来时 LMCache reload、CacheGen 解码回张量，快于重 prefill、也快于搬原始张量 → TTL 到期或程序末步释放。**三篇无人在 agent 工具调用场景下串成这条管线**——这是 Mimir 的贡献：集成 + 场景化 + 诚实定位。

**为何 CacheGen 是关键拼图**：naive 的 Continuum+LMCache（=Continuum 论文已做的）offload 整个未压缩张量，走 PCIe/网络慢，尤其长上下文/低带宽场景。CacheGen 把 offload 的 KV 压成 bitstream，reload 快一个量级，补上这环——使"显存换重算"的权衡在低带宽下依然成立（否则 reload 慢到不如重 prefill）。

**归因（诚实）**：Continuum/LMCache/CacheGen 均为 credited prior work，Mimir 移植+集成+场景化，不占为己有。对外这套机制统称 **Mimir**。KVFlow（arXiv 2507.07400）走第四个轴（"哪个 agent 该淘汰"，steps-to-execution），作为 related work 提及，不做集成。

**为什么是 3090/Agent 架构强关联**：3090（Ampere SM_86）无 fp8 硬件，单卡 KV 容量无法靠精度提升，只能靠"把暂时不用的 KV 压缩搬走、要时快速取回"。agent 工具调用场景天然命中这条管线的收益点：工具调用暂停时 KV 暂不用 → 压缩 offload；下一步回来 → 解码 reload，远好于重 prefill（尤其长上下文）。这正对应 CacheGen 长上下文 3.2–3.7×、LMCache 多轮 QA 15× 的收益模式。

> **状态（三篇均已落地验证）**：
> - **Continuum TTL**（何时）已 port 进 `"mimir"` 策略并验证（commit d9070f3，13 单测 + 真引擎冒烟：工具调用→pin→末步释放全生命周期跑通）。
> - **LMCache**（搬哪）已集成（commit 97f882f，`src/mimir/lmcache_compat.py`）：三件套（otel provider 兜底 + connector 自注册 + 可用性报告）+ engine 按 `extra["lmcache"]=True` 接入。真引擎冒烟（Qwen3-4B + mimir 策略 + LMCache）三件套共存跑通：policy=MIMIR、TTL est 就绪、lmcache available(0.4.7/vllm 0.10.2/connector_registered)。Continuum TTL 与 LMCache 松耦合：TTL 决定何时留/放，LMCache 负责 store/reload。
> - **CacheGen**（怎么压）已验证集成：CacheGen 编解码器（delta+分层量化+算术编码）**已随 LMCache 0.4.7 一并发布**（`lmcache.v1.storage_backend.naive_serde.cachegen_encoder.CacheGenSerializer`），作为 LMCache 的 storage serde 后端。`tests/test_cachegen_serde.py` 验证：对 Qwen3-4B 真实 KV 形状（2,36,256,1024 bf16）CacheGenSerializer/Deserializer 正确 round-trip、形状还原，**压缩 2.88×**（合成张量；论文真实 KV 因 token-wise locality 达 3.5–4.3×）。模型名走 `CacheGenConfig.from_model_name` 的 AutoConfig 默认回退（Qwen3-4B: 36 层，前 10 层 K/V 用 32 bins、后续 16 bins）。
>
> **关于早期 block-class**：此前实现过"按语义角色给 KV 块打标签、按类别淘汰"的 block-class 机制（`block_pool.mimir_class_aware_evict` 等，5 单测）。经重审，三篇融合的定位下不再以 block-class 为核心创新（其"按价值差异化对待"的思想已由 CacheGen 的分层量化覆盖，且无独立正交贡献），相关代码暂留备查但不作为卖点。

**验证**：
- Continuum TTL port — `tests/test_continuum_port.py`（13 例）+ 真引擎冒烟（step1 工具调用→pin、step2 末步→释放）。
- LMCache 集成 — `src/mimir/lmcache_compat.py` + 真引擎三件套共存冒烟。
- CacheGen 编解码 — `tests/test_cachegen_serde.py`（round-trip + 压缩比 2.88× + 小于 naive）。
- 详见 §5 与 `VLLM_PATCH_INVENTORY.md`。

**诚实边界（实测后更新）**：三篇机制均已正确落地并验证（见上）。端到端收益的量化做了实测
（详见 [`三篇融合评测与边界.md`](三篇融合评测与边界.md)），结论：
- **机制正确性已证**：`run_target_interferer.py` 实测 mimir 侧 `max_pinned=1`（强制工具调用 prompt
  下 step1 发 `[TOOL:...]`、TTL pin 触发），native=0；冒烟亦证 step1→pin、step2 末步→释放。
- **端到端收益受设置限制**：我们用 vLLM v1 **InprocClient 同步**模式（为可观测 block_pool/scheduler），
  `LLM.chat` 串行化、无法在线交错（多线程并发触发 `Forward context is not set` 崩溃）。Continuum TTL
  的在线交错收益需**异步 server + 持续请求涌入**（Continuum 论文即用 `vllm serve` 异步）；我们同步设置
  给不了这个场景 —— 这是**设置限制，非机制缺陷**。20 agent×5 轮×1500 ctx 下显存争用已构造出来
  （hit 0.74→0.50、TTFT 175→1209ms、31.5K token 重算），但同步批量 gap 期间无重叠，TTL 与 native 无差。
- **CacheGen 收益**：靠低带宽/远端驱动（非显存争用），单卡本地 PCIe 走 LMCache 收益在省 CPU 内存/KV size，
  TTFT 收益需远端 `remote_serde="cachegen"` 才体现（LMCache 论文同结论）。
- **获得收益数字的诚实路径**：切异步 server（`vllm serve` + Continuum TTL + LMCache + CacheGen serde）+
  BFCL/SWE-Bench 真实轨迹 + Poisson 涌入，跑 P90/P95 job duration —— 代价是放弃 InprocClient 可观测性。

## 4. 系统集成：两层优化 + vLLM 内核级 patch

Mimir 的优化分两层，通过统一入口 `MemoryManager` 串起：

### 4.1 外部层（请求侧变换，`src/mimir/`）

在把请求喂给 vLLM 之前改写：
- **上下文压缩**（`mimir.context`）：摘要旧对话轮次 + 精简工具描述（保真度可调：LOSSLESS/BALANCED/AGGRESSIVE）——真实减少进 KV 的 prompt。
- **工具数据外置**（`mimir.tools`）：工具返回的大对象（~5KB JSON）存入 `ToolDataStore`，上下文仅放轻量引用 + 摘要；按需 `materialize()` 取回——真实减少必需 KV。
- **分层存储**（`mimir.tiered`）：GPU/HOST/DISK 三层冷热分离，自动 demote/promote——避免 OOM。
- **分支 CoW 记账**（`mimir.branch`）：分支树 + 写时复制，统计共享节省。
- **硬件抽象**（`mimir.hardware`）：CUDA/ROCm/Ascend/Cambricon/CPU 降级链。

> 注：早期版本的「生命周期淘汰/任务边界主动回收」（mimir.kv_cache 的 finish_task）已删除——其 used_blocks→0 系任务结束后瞬时计数偷换（推理时该占仍占、回收重算拖慢服务），已彻底移除。Mimir 的显存收益回归工具外置/压缩真实减少必需 KV、分层避免 OOM。

### 4.2 引擎层（vLLM v0.10.2 in-tree patch，`third_party/vllm/`）

直接修改 vLLM 源码（纯 Python，不重编 `_C`），把优化下沉到引擎内核：
- `kv_cache_manager.get_computed_blocks`：跨分支 CoW 复用记账。
- `arg_utils` fp8 优雅降级（不支持硬件降级 bf16 而非崩溃）。
- **【创新】`block_pool.mimir_block_class` + `mimir_class_aware_evict()`**：per-block 语义类别标签
  + 按类别优先级淘汰（详见 §3.7）——3090 无 fp8 下靠「谁该被淘汰」更聪明提升有效 KV 容量。
- `output_processor`（Phase R）：v1 TTFT 可观测回填。

> 注：原 lifecycle 主动回收 / per-block pin / mimir 调度策略自驱动回收已删除（used_blocks→0 系偷换概念）。

详见 [`VLLM_PATCH_INVENTORY.md`](VLLM_PATCH_INVENTORY.md)。

### 4.3 统一入口

```python
mm = MemoryManager(features=["context_compress", "tool_offload", "tiered"])
eng = VLLMEngineV1(cfg, device=0)  # 内核 Continuum-TTL + CoW 记账（mimir 策略）
r = mm.run_turn_with_engine(eng, case, task_id="t1")
# r["engine_stats"] 的 used/total/CoW + pinned_requests 可观测
```

## 5. Benchmark 与评估方案

### 5.1 Agent-Loop Benchmark（核心评测）

不是预构造消息，而是让 LLM 真正驱动多步任务循环：
LLM 生成 → 解析 tool_call → 执行 mock 工具 → 结果喂回 → LLM 再生成 → ...

3 个任务（research_kv_cache / compare_frameworks / multi_step_estimate），每个最多 8-10 步。

A/B 对照：同一任务/种子/GPU：
- **native**：fcfs 策略，工具结果全量进 KV，无 TTL 保留、无 offload
- **Mimir**：mimir 策略 + 工具调用边界 TTL 保留（Continuum，已 port）+ LMCache offload（已集成）
  + CacheGen 压缩（已验证）+ 工具外置/压缩（外部层，已落地）。三篇机制跑通，
  并发多轮 agent 的端到端收益量化 benchmark 为下一步（见 §3.7 诚实边界）。

指标：每步 `used_blocks` / `ttft_ms` / `e2e_latency` + `peak_used_blocks` + TTL 命中/未命中、
reload 次数（offload 集成后）。

```bash
python scripts/run_agent_benchmark.py --tool-kb 5 --max-tokens 64
```

### 5.2 内核层 patch（Continuum TTL 已 port + CoW/TTFT/fp8 支撑）

回收机制已删除（used_blocks→0 系偷换概念，见测试报告 §5.0）。内核层 patch：

| 内核机制 | 说明 | 状态/实测 |
| --- | --- | --- |
| **Continuum TTL 保留**（创新核心之一） | 工具调用边界 pin KV、TTL 到期/末步释放、program-FCFS、死锁兜底 | **已 port 验证**：13 单测 + 真引擎冒烟（工具调用→pin→末步释放） |
| CoW 跨分支复用（Phase D） | 分支共享前缀记账 | 4 分支 9 次复用 |
| v1 TTFT 可观测（Phase R） | 回填 RequestMetrics | 每请求 TTFT/prefill 可读 |
| fp8 优雅降级（Phase F） | 不支持硬件降级 bf16 | 3090 上不崩 |
| ~~block-class 类别淘汰（Phase BC）~~ | 早期实验，按语义角色淘汰 | 已弃用为核心（见 §3.7 注），代码暂留备查 |

### 5.3 task-call probe 召回率（早期 block-class 实验的诚实指标，**已不作核心**）

> 注：本节为早期 block-class 实验的诚实指标，block-class 已弃用为核心创新（见 §3.7 注），保留此节作为历史备查。融合管线下的正确性代理将由 DeepSeek LLM-judge（§5.4）承担。

构造 agent 场景：长上下文含「工具调用 probe」（key 埋在 tool_result）+ 大量 reasoning 干扰。
KV 压力下对比 native(LRU) vs Mimir(早期 class-aware 优先淘汰 reasoning)：

| 路径 | 第二次命中 token | 说明 |
| --- | --- | --- |
| native(LRU) | 1088 | 盲淘汰，probe 块部分被挤 |
| Mimir(class-aware) | 1136 | 主动淘汰 342 reasoning 块、tool_result 完整存活 |

「正确性」代理：probe 二次命中越多=agent「越没失忆」。策略正确性由 `tests/test_block_class.py` 5 单测保证。

### 5.4 DeepSeek V4 Pro 真实轨迹 + LLM-judge 评估

**真实轨迹**：用 DeepSeek V4 Pro 充当 agent 跑 mock 工具，产 3 条真实多步轨迹（9-10 步、
工具数据 21-64KB），作为 benchmark 工作负载——相比弱模型（Qwen3-4B）自驱动的轨迹更真实，
replay 时 native/Mimir 跑**同一份上下文**，A/B 干净。

| 任务 | native | Mimir | native 峰值 used | Mimir 峰值 used | 能否跑完 |
| --- | --- | --- | --- | --- | --- |
| compare_frameworks | 崩 | 3 步 | 285 | **0** | native 溢出崩，Mimir 跑完 |
| multi_step_estimate | 7 步 | 7 步 | 763 | **0** | 都跑完，Mimir 显存稳态 0 |
| research_kv_cache | 10 步 | 10 步 | 1235 | **0** | 都跑完，Mimir 显存稳态 0 |

Mimir 全程 `used_blocks=0`；native 在重轨迹下溢出崩溃，轻轨迹虽跑完但 KV 堆到 763/1235 块逼近 1780 池上限。崩溃场景匹配步 TTFT −86%（compare 316 vs 43 ms）。诚实 trade-off：低强度回放下 Mimir offload 使前缀 hash 改变、APC 命中率下降，TTFT 与 native 持平或略高——显存换重算，价值在显存维稳态 0 + 不崩。

**LLM-judge 保真 A/B**：用 DeepSeek-flash 作裁判，对 Qwen3-4B 在 full vs Mimir 压缩上下文
（−91%~−97%）下回答分别评分（相对参考答案 0-10）：

| 任务 | full 上下文 | Mimir 压缩上下文 | 说明 |
| --- | --- | --- | --- |
| compare_frameworks | 10/10 ✅ | 10/10 ✅ | 压缩无损 |
| multi_step_estimate | 0/10（65K 字符 > max_model_len 崩）| 0/10 | 压缩后能跑但未给出估算 |
| research_kv_cache | 0/10（62K 字符崩）| 见 judge | full 直接溢出崩 |
| **平均** | 3.33 | 5.0 | **压缩无损**·full 上下文超长直接崩，Mimir 压缩后反能答 |

结论：上下文压缩对能跑的场景无损（compare_frameworks 10/10 == 10/10）；对 full 上下文
超 `max_model_len` 直接崩的场景，Mimir 压缩使其可运行——**压缩不仅是省显存，更是救任务**。

### 5.5 模型

- **主力**：Qwen3-4B-Instruct-2507（本地已有）
- **泛化**：Qwen3-1.7B + Qwen3-8B（1.7B/4B 已验证 Continuum TTL + CoW）

## 6. 风险与权衡

| 风险 | 应对 |
| --- | --- |
| 上下文压缩可能损失信息 | 保真度可配置；task-success delta=0 验证不降质量 |
| 分层存储带来额外访存延迟 | 冷热分级；热数据常驻显存 |
| vLLM 内部改动维护成本 | 纯 Python patch（不重编 `_C`）；mimir_* 方法均为 additive |
| fp8 在不支持硬件上 | Phase F 优雅降级 bf16（原会崩溃） |
| 工具外置后 LLM 看不到完整数据 | 按需 materialize()；当前 mock 工具验证 |
| TTL 保留/分层 offload 收益依赖显存争用 | 单 agent 顺序跑无争用则收益≈0；benchmark 用并发多轮 agent 场景，如实报数 |
| 短上下文+低 PCIe 带宽下 reload 不赢 prefill | LMCache 自身结论：32Gbps 下需 >256K token 才赢；agent loop 靠 tool_result 复用放大收益，如实标注边界 |
